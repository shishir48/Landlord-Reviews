/**
 * App configuration.
 * Edit these values directly for your environment.
 * In production, use react-native-config or similar.
 */
export const Config = {
  API_URL: 'http://10.0.2.2:3000', // Android emulator → host machine
  // For physical device, use your machine's LAN IP, e.g. 'http://192.168.1.5:3000'

  FIREBASE_API_KEY: 'your-firebase-api-key',
  FIREBASE_AUTH_DOMAIN: 'your-project.firebaseapp.com',
  FIREBASE_PROJECT_ID: 'your-project-id',
  GOOGLE_WEB_CLIENT_ID: 'your-oauth-client-id.apps.googleusercontent.com',
};